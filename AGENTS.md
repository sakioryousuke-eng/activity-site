# activity-site 作業ルール

## 基本方針

- このリポジトリは Jekyll で構築され、GitHub Pages の `/activity-site` 配下で公開される。
- 内部ページ・画像・CSS・PDFへのURLには、原則として Jekyll の `relative_url` を使用する。`/activity-site` をテンプレートへ直接埋め込まない。
- 既存のUI、配色、文字サイズ、余白、カードサイズ、配置、レスポンシブ挙動を、明示的な指示なしに変更しない。
- 「改善のため」という理由だけで、依頼に不要なデザイン変更や仕様変更を行わない。
- 依頼された目的と直接関係しないリファクタリング、ファイル移動、名前変更、共通化を行わない。
- 変更範囲は必要最小限とし、既存コンテンツを保持する。

## 重要な構成

- トップページ `/` は `index.html` であり、現在のプロフィール本体でもある。
- `pages/profile.html` はナビゲーションから使用されていない旧ページである。トップページと混同しない。
- 活動データは `_posts/` から取得する。`_data/activity.yml` は現在実質未使用である。
- 公約データは `_data/promises.yml`、一般質問データは `_data/matrix.yml` で管理する。
- 全ページ共通レイアウトは `_layouts/default.html` である。
- 記事ページは `_layouts/post.html` を使用し、さらに `default.html` を親レイアウトとする二段構成である。
- 共通CSSは `assets/css/ui.css` である。ページ固有のCSSやJavaScriptは各ページ内にも存在する。
- `_includes/` は現在存在しない。明示的な依頼なしに新たな共通化を導入しない。

## ナビゲーション

- ナビゲーションは `_layouts/default.html` に直書きされている。
- ヘッダーとフッターの両方に、プロフィール、公約、活動、一般質問、検索、タグの同じ6項目が存在する。
- ナビ項目を変更する場合は、ヘッダーとフッターを必ず同期する。
- リンク変更時は、`page.url` と `aria-current` を使った現在ページ判定も同時に確認・更新する。
- `default.html` の変更は全ページへ影響する。指示された箇所以外を変更しない。

## UIとレスポンシブ表示

- 現在のナビはPC・スマートフォンとも6項目で、PCでは3列×2段で表示される。
- 既存のUIサイズ、余白、配置、カード表現を独自判断で変更しない。
- ページ固有CSSを追加する場合は、そのページだけに作用するスコープされたクラス名を使う。
- `default.html`、`assets/css/ui.css`、各ページ内に分散している既存のレスポンシブ処理を維持する。
- 共通クラスを変更する前に、他ページへの影響を確認する。

## PDF

- 共通PDFモーダルのHTML、CSS、JavaScriptは `_layouts/default.html` にある。
- PDFモーダル処理は、明示的な指示なしに変更しない。
- PDFリンクでは既存仕様を維持し、用途に応じて `.pdf-link`、`data-pdf`、または `data-modal="pdf"` を使用する。
- 既存PDFや画像を名前変更・移動する場合は、全参照箇所の更新が明示的に求められているか確認する。

### 要点資料のデザイン

- 一般質問の要点資料は、重点課題PDFと統一感のある紫、白、淡い紫を基本色とする。
- ページ上端の黄緑とオレンジのアクセントラインを共通要素として使用する。
- オレンジは「最重要の到達点」など、資料内で最も伝えたい内容の強調に限定する。複数箇所を同じ強さで強調しない。
- 1段目は「問題・質問・市の答え」を一目で理解できる構成とし、必要に応じて図式や短い流れを併用する。
- 2段目は「方針・必要な接続・未定や課題・今後の確認」を区別し、決定事項と提案、未決定事項を混同しない。
- 相談件数や制度の開始だけを成果とせず、学び、居場所、福祉、就労など、実際に社会との接点へつながったかを確認できる表現を優先する。
- 書体、余白、カード表現は既存の重点課題PDFを基準とし、独自の色や装飾を増やさない。

## 検索とタグ

- 新しい内容を検索対象へ追加する場合は `search.json` の生成処理を確認する。
- 新しい内容をタグ対象へ追加する場合は `pages/tags.html` を確認する。
- 検索・タグへの追加は、ユーザーの指示がある場合のみ行う。

## Git・確認・公開

- 作業開始前と作業終了後に `git status` を確認する。
- コミット前に `git diff --name-only` と `git diff` を確認し、依頼対象以外の変更がないことを確認する。
- Jekyllビルドまたはローカル表示で、Front MatterとLiquidが正しく処理されることを確認する。
- UI変更時はPC幅とスマートフォン幅の両方を確認する。
- 意図しないファイル変更、生成物、無関係な整形が含まれている場合はコミットしない。
- `_site/`、`.jekyll-cache/` などの生成物を編集・コミットしない。
- コミット、push、公開反映は、ユーザーから明示的に依頼された場合のみ行う。
- 「ホームページに反映」「公開して」「GitHubへ反映」などの明示的な依頼は、コミット、push、公開確認までの一括許可として扱う。公開直前に同じ内容の許可を再度求めない。
- 公開依頼を受けた場合も、対象外の差分がある、リモートに未取得の変更がある、競合がある、または通常のpushが拒否された場合は自動解決や強制pushをせず停止して報告する。
- 「ローカル確認まで」「コミット・pushしない」などの指示がある場合は、上記の一括許可よりその指示を優先する。
- Windows自動公開が有効な環境では、公開対象をコミットした後、work/publish-tool/request-publication.ps1 -ExpectedCommit <HEADの完全なコミットID> を実行して公開依頼を登録する。Codex環境から直接pushできない場合も、ユーザーへターミナル操作を求めない。
- 自動公開の結果は work/publish-tool/publish-result.json で確認する。status が success になるまで待ち、その後GitHub Pagesの公開表示を確認する。
- 自動公開が error になった場合だけ、結果の内容を調査する。強制pushや自動競合解決は行わない。
